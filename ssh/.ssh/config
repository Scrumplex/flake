ControlMaster auto
ControlPersist 300
ControlPath ~/.ssh/sockets/sockets-%r@%h:%p

Include hosts/home.conf
Include hosts/scrumplex.conf
Include hosts/work.conf

Host aur aur.archlinux.org
        User            aur
        Hostname        aur.archlinux.org
        IdentityFile    ~/.ssh/git

Host gitlab gitlab.com
        User            git
        Hostname        gitlab.com
        IdentityFile    ~/.ssh/git

Host srht git.sr.ht
        User            git
        Hostname        git.sr.ht
        IdentityFile    ~/.ssh/git

Host github github.com
        User            git
        Hostname        github.com
        IdentityFile    ~/.ssh/git

Host freedesktop gitlab.freedesktop.org
        User            git
        Hostname        gitlab.freedesktop.org
        IdentityFile    ~/.ssh/git

