#!/usr/bin/env fish

if [ "$START_BACKUP" = "1" ]
    borgmatic --stats --progress; or true    
    read -P "Press enter to exit... "
else
    screen -dmS borgmatic env START_BACKUP=1 automatic-backup
end

